# Copy to Vercel project env vars (or .env.production for local builds).
# Replace with your actual Railway backend URL after Phase 3.
VITE_API_URL=https://your-backend.up.railway.app
VITE_ENVIRONMENT=production
